# Upload Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: agrocd-sync
  name: upload-service
  labels:
    app: upload-service
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: upload-service
  template:
    metadata:
      labels:
        app: upload-service
    spec:
      serviceAccountName: upload-service-sa
      containers:
      - name: upload-service
        #image: daasnigeria/daasrepo:upload-service
        image: igwubor/project_portal-upload-service:v1.2.3
        imagePullPolicy: IfNotPresent  # Pull from registry
        ports:
        - containerPort: 8083
        env:
        - name: GIN_MODE
          value: "release"
        envFrom:
        - configMapRef:
            name: app-config
        volumeMounts:
        - name: uploads-storage
          mountPath: /root/uploads
        resources:
          limits:
            memory: "256Mi"
            cpu: "200m"
          requests:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          tcpSocket:
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: 8083
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: uploads-storage
        persistentVolumeClaim:
          claimName: uploads-pvc
---
# Upload Service Service
apiVersion: v1
kind: Service
metadata:
  name: upload-service
  namespace: agrocd-sync
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: upload-service
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP